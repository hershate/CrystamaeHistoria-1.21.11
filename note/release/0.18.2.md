# CrystamaeHistoria 0.18.2 发布说明

**版本**：`0.18.2`
**基线**：`0.18.1`（稳定性/正确性修复版——审计第 34-66 轮收口）
**日期**：2026-08-19
**性质**：补丁版——审计第 71/72 轮发现并实证的单项修复。

## 本版变更（1 项修复）

1. **姿态克隆器 `onPoseClone` 缺 `ignoreCancelled`**（`293ef90`，r71
   事件优先级全景矩阵发现）：被其他插件否决的
   `PlayerInteractAtEntityEvent` 仍会进入处理器执行姿态克隆与物品
   消耗——与同文件姊妹处理器 `onPoseChange` 不一致，属 r25 同类
   缺陷（红线 4：PlayerInteract 系事件须 `ignoreCancelled=true`）
   的漏网处。一行修复。
   **实机验证（r72 消息哨兵双对照）**：未取消事件处理器正常执行
   （哨兵 #1 到达，克隆功能不变）；预取消事件处理器跳过（无第二条
   消息）——修复精确实证。

## 验证摘要

- 修复经真实事件分发双对照验证（`note/audit/round-72.md`）；
- 发布前连续判定轮零发现（r73-r86 十四轮角度互异矩阵，事件一致性
  立方体 r71/r74/r86 拼合）。

## 兼容性

- 无数据格式变更、无配置格式变更、对外 API 不变；旧存档完全兼容；
- 无用户可感知行为差异（仅被否决交互不再误执行）。

## 构建

`JAVA_HOME=F:/Java/21 mvn -q package`；产物
`target/CrystamaeHistoria-0.18.2.jar`。
