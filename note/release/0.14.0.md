# CrystamaeHistoria 0.14.0 发布说明

**版本**：`0.14.0`
**基线**：`0.13.0`（第十二轮性能优化版——生成预配置与复用域）
**日期**：2026-08-17
**性质**：第十三轮性能优化版——**判定收敛版（r66-67）**。
**本版无代码变更**（两轮判定轮连续零发现，按循环准则收口版本与
文档状态；如实标注）。

## 判定总览（逐轮报告见 perf 索引）

| 轮次 | 角度 | 结果 |
|------|------|------|
| 66 | 玩家集合视图克隆 / teleport 形态 | 零发现（`world.getPlayers()` 克隆形态零命中；6 处 teleport 均同世界主线程正确形态） |
| 67 | 异步任务形态 / getRelative 遍历 | 零发现（CompletableFuture/async 零命中——全程同步主线程；22 处 getRelative 循环内站点全为已优化/特性语义/已判定域） |

r66+r67 连续两轮零发现且角度互异——按 round-44 准则宣告**第十三轮
循环收敛**。

## 兼容性

- 无代码变更、无数据格式变更、对外 API 与 `depend`/`softdepend`
  与 0.13.0 完全一致；产物仅版本号差异。

## 验证

- `JAVA_HOME=F:/Java/21 mvn package` 构建通过，产物
  `target/CrystamaeHistoria-0.14.0.jar`；
- Paper 1.21.11 build 132 + Slimefun 5.0.0 实机全量终验
  （round-67-final 会话）：**223 变体，40 条等价性断言行中 3 处
  false 均为文档化预期**（round13/24/35，与历次终验完全一致），
  0 真实插件异常（首次会话因基准插件未回部署而空跑，属基础设施
  步骤而非产物问题，补部署后复测通过——如实记录）；
- 纯空闲 soak 10 分钟：0 异常 0 tick 落后 0 watchdog
  （soak_r67.log，同历次口径）。

## 循环状态

累计十三轮循环、67 轮（0.1.0 起各域逐轮收敛：事件/世界级/写路径/
展示/编码/统计/落盘/复查长尾/事件门控/粒子常量化/生成预配置/判定
收敛）。插件侧性能面形态维持：结构域与惯用法长尾闭合，剩余为 API
边界、事件级边际与平台架构边界三类。重启触发条件不变：Paper/
Slimefun 演进、玩法变更、集成环境实测画像。
