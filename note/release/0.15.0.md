# CrystamaeHistoria 0.15.0 发布说明

**版本**：`0.15.0`
**基线**：`0.14.0`（第十三轮性能优化版——判定收敛版）
**日期**：2026-08-17
**性质**：第十四轮性能优化版——**判定收敛版（r68-69）**。
**本版无代码变更**（两轮判定轮连续零发现，按循环准则收口版本与
文档状态；如实标注）。

## 判定总览（逐轮报告见 perf 索引）

| 轮次 | 角度 | 结果 |
|------|------|------|
| 68 | 枚举键 Map 容器选型 / 不可变集合构造 | 零发现（全库一致 EnumMap；EnumSet·List.of 全事件级） |
| 69 | Objects.hash varargs / 自定义 hashCode / Map 键哈希 | 零发现（Objects.hash 零命中；唯一自定义 hashCode 为 String 委托最优；BlockPosition 为 dough 库值类边界） |

r68+r69 连续两轮零发现且角度互异——按 round-44 准则宣告**第十四轮
循环收敛**。连续两轮纯判定收敛循环（r66-67、r68-69）表明复查节奏
边际产出稳定为零，**循环进入休眠**；重启触发条件（Paper/Slimefun
演进、玩法变更、集成环境实测画像）沉淀于 note/README.md。

## 兼容性

- 无代码变更、无数据格式变更、对外 API 与 `depend`/`softdepend`
  与 0.14.0 完全一致；产物仅版本号差异（功能代码与 0.13.0 起一致）。

## 验证

- `JAVA_HOME=F:/Java/21 mvn package` 构建通过，产物
  `target/CrystamaeHistoria-0.15.0.jar`；
- Paper 1.21.11 build 132 + Slimefun 5.0.0 实机全量终验
  （round-69-final 会话）：**223 变体，40 条等价性断言行中 3 处
  false 均为文档化预期**（round13/24/35，与历次终验完全一致），
  0 真实插件异常（CH 错误行 1 次为 watchdog 转储堆栈帧伪象，
  r49 起已知口径）；
- 空闲 soak：**因用户终止收尾未跑满 10 分钟标准口径**——已完成的
  约 6 分钟空闲段内 0 watchdog、0 SEVERE、0 插件告警、0 tick 落后，
  0.15.0 正常加载（soak_r69-partial.log，部分日志如实归档）；鉴于
  本版与 0.13.0/0.14.0 功能代码完全一致（该代码已两次通过完整
  10 分钟 soak），风险敞口为零。

## 循环状态（本次工作收尾）

累计十四轮循环、69 轮（0.1.0 起：事件/世界级/写路径/展示/编码/
统计/落盘/复查长尾/事件门控/粒子常量化/生成预配置/判定收敛 ×3）。
插件侧性能面形态终态：结构域与惯用法长尾闭合，剩余成本归属 API
边界、事件级边际与平台架构边界三类。本次会话按用户指令收尾，
循环休眠待触发。
