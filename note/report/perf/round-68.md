# 性能优化第 68 轮：判定轮——枚举键容器选型/不可变集合构造探针（零发现）

日期：2026-08-17
性质：判定轮（第十四轮循环收敛计数 1/2，0.14.0 后用户再触发开启）。
探针角度与 r42-67 互异：**枚举键 Map 容器选型、热路径不可变集合构造**。

## 探针结果（全库扫描 + 逐点复核，零可行动发现）

1. **枚举键容器选型**：全库枚举键 Map 一致使用 `EnumMap`（16 个
   文件；核心的 `InstanceStave.spellInstanceMap` 即 EnumMap 数组
   索引形态）——**无 HashMap 承载枚举键的站点**，容器选型全程
   正确；
2. **不可变集合构造**：`EnumSet.of`/`List.of`/`Set.of` 命中集中在
   `LiquefactionBasinCache`（配方索引键/三型集合/展示列表），全部
   位于**催化剂交互事件级**（每次交互构造一次，~百 ns）；r11 配方
   索引后查找已 O(1)，键构造为交互固有成本——事件级边际，判定
   不做。

## 判定

本轮零发现（两组探针全空）。第十四轮循环收敛计数 **1/2**；下轮
（r69）以新角度复核，若再零发现则按 r44 准则宣告第十四轮循环收敛
并执行下一版本收口。
