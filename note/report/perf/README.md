# 性能优化轮次索引（/loop 持续性能优化）

目标：在**安全性、稳定性、兼容性红线**（详见 [../../README.md](../../README.md) 维护要点）
约束下持续优化性能。每轮从不同优化点出发，量化前后（[benchmark/](../../../benchmark/)），
服务器回归验证后落盘。

**红线口径（用户澄清，2026-08-16）**：兼容 = **用户体验一致 + 对外
（供其他插件调用的）API 不变**；内部实现/内部调用可自由重构，
不必逐方法等价。

## 轮次状态

| 轮次 | 优化点 | 状态 |
|------|--------|------|
| 1 | 施法前置校验缓存（spellEnabled 29x）+ SpellMemory 周期清理零复制扫描（空表 >4000x / 满表 8x）（[round-1](round-1.md)） | ✅ 完成（服务器回归通过） |
| 2 | 施法触发路径：懒 raycast + 冻结 + 事件顺序重排（失败路径 0 raycast；成功 1.7-2x；未映射动作免 5.4μs PDC）（[round-2](round-2.md)） | ✅ 完成（服务器回归通过） |
| 3 | 交互路径 ItemMeta 削减：材质预检（冷却检查 8.7x / 调光勺 3.6x，每次右键/交互全局生效）+ 施法写回合批（[round-3](round-3.md)） | ✅ 完成（服务器回归通过） |
| 4 | 机械 tick 路径：单次元数据判定链（记录者面板稳态 2.24x）+ 拾取点预计算（[round-4](round-4.md)） | ✅ 完成（服务器回归通过） |
| 5 | 机械 tick 判定备忘录（实例引用比较，稳态判定 2719→2.6ns，1034x；消除 JSON 解析地板）（[round-5](round-5.md)） | ✅ 完成（服务器回归通过） |
| 6 | 法杖 PDC 单槽局部读取 + 单次 meta 克隆（失败前置路径 1.5-1.7x；成功路径净下降）（[round-6](round-6.md)） | ✅ 完成（服务器回归通过） |
| 7 | gadgets 每 tick 模式清扫（7 类：字符串解析缓存 2.3x / 材质数组 5.9x / Location 分配 2.7x + 粒子常量/循环内查询消除）（[round-7](round-7.md)） | ✅ 完成（服务器回归通过） |
| 8 | 故事选取稀有度×类型索引（22.9x）+ 配置加载双解析消除（blocks.yml 2.31x）（[round-8](round-8.md)） | ✅ 完成（服务器回归通过） |
| 9 | 统计路径 MessageFormat 消除（12.4x）+ v0.2.0 全量终验 + 10 分钟 soak + **收敛判定**（[round-9](round-9.md)） | ✅ 完成（**循环闭合**） |

## 第二轮循环（完全重写授权，2026-08-16 起）

| 轮次 | 优化点 | 状态 |
|------|--------|------|
| 10 | 世界级高频事件路径 O(1) 化：弹射物/下落方块反查 UUID 索引（空表 20x / 满表 38.5x）+ 无敌标记内存注册表（检查 4.3x / 写入 5.7x）+ 召唤物类型门控（5.0x）（[round-10](round-10.md)） | ✅ 完成（服务器回归通过） |
| 11 | 液化池路径全套：syncBlock 脏标记落盘（431x）+ 配方类型集索引（miss 140x / hit 27x）+ top-3 单遍选取（7.5x）+ 液位懒缓存 + tick 分配消除（[round-11](round-11.md)） | ✅ 完成（服务器回归通过） |
| 12 | 启动路径（冷路径）：稳态零配置落盘（16x）+ 首次启动批量补键（63.8x）+ 故事解析单读（2.1x）+ onEnable 分段计时画像 + 基准框架链式调度修复（[round-12](round-12.md)） | ✅ 完成（服务器回归通过） |
| 13 | 剩余全局监听器门控审计（17 个收官）：isStoried 元克隆门控 5.8x（×4 处）+ 隐藏器 PAPER 门控 1.9x + 禁刷区空表早退；含等价性运行时断言与不做项论证（[round-13](round-13.md)） | ✅ 完成（服务器回归通过） |
| 14 | v0.3.0 全量终验（69 变体）+ soak（0 异常 0 tick 落后）+ **收敛判定**（[round-14](round-14.md)） | ✅ 完成（**第二轮循环闭合**） |

## 第四轮循环（展示与图鉴域，2026-08-16 起）

| 轮次 | 优化点 | 状态 |
|------|--------|------|
| 21 | 图鉴 GUI 展示路径：法术集/故事集/镀金集翻页排序快照（spellPageSort 541.0x / blockPageSort 10,946.9x，另消除共享缓存数组原地污染）+ 主题堆/图标/详情堆记忆化（themedStack 225.9x / 页 36 图标 182.0x / 详情堆 1,421.1x）+ TitleCase 缓存（4.5x）；六组服务器内等价性断言全 true（[round-21](round-21.md)） | ✅ 完成（服务器回归通过，0 SEVERE） |
| 22 | 玩家统计读取路径：图鉴页级统计子节单次解析 + 36 槽相对读取（页判定 2.06x，服务器内真实 PlayerStatistics）+ 三个解锁计数内层相对读取（服务器内 69 键 2.23x / 故事键 1.76x；standalone 计数比率 1.89-1.95x，原 6.3x 为基准除数缺陷已更正）；逐键等价性断言全 true（[round-22](round-22.md)） | ✅ 完成（服务器回归通过，0 SEVERE） |
| 23 | 统计计数纪元缓存：全部 6 个统计写点递增纪元（已核验无外部写者），三个计数方法字段级纪元缓存——服务器内 274+ 键计数 68.9µs → 8.77ns 命中（~7,859x vs 相对 / ~13,820x vs 全路径），液化池 rank 谓词稳态 11.17ns（>6,000x）；失效正确性经真实写方法断言（解锁后计数 +1）（[round-23](round-23.md)） | ✅ 完成（服务器回归通过，0 SEVERE） |
| 24 | lore 展示写入组件化——**试行后回退（阴性结果）**：`lore(Components)` 实测比 `setLore(Strings)` 慢 48-133%（Paper 1.21.11 组件应用路径更重），且纯转换仅 ~132ns/行（原瓶颈归因错误，18-21µs 主体为 ItemMeta 应用机制 = API 边界）；等价诊断 strings=true/components=false。基准证据保留（[round-24](round-24.md)） | ✅ 完成（回退后代码回归通过，0 SEVERE） |

## 第三轮循环（写路径与提交域，2026-08-16 起）

| 轮次 | 优化点 | 状态 |
|------|--------|------|
| 15 | 写路径单次元数据往返归一：故事提交（8-10 次克隆 → 1，2.47x）+ 终格独特提交（1.54x）+ 祭坛提取（1.30x）+ 统计计数单路径（1.17x）+ 法杖 lore 静态片段（1.08x）；四组终态等价性运行时断言全通过（[round-15](round-15.md)） | ✅ 完成（服务器回归通过，0 插件异常） |
| 16 | 展示行构建缓存：Story.getDisplayName/getStoryLore 记忆化（输入不可变、实例全局共享）——displayName 49.5x / loreLines 40.6x / 4 条组装段 9.9x；跨稀有度采样等价性 true（[round-16](round-16.md)） | ✅ 完成（服务器回归通过，真实插件异常 0） |
| 17 | 召唤物 AI 每 tick 路径：冲撞车块扫描 13.5x（O(n²) 去重为无效工作）+ 主人查询 5.4x（三步 OfflinePlayer → 单次 getPlayer）+ GoalType 集共享常量 3.1x；四组等价性断言 true；targetReads 如实记为噪声级（[round-17](round-17.md)） | ✅ 完成（服务器回归通过，0 插件异常） |
| 18 | 法术周期效果与周期任务路径收官：TunnelBore 块扫描 O(n²) 去重消除（等级 5 施法每 tick 3.24ms → 12µs，271.4x；等级 3 76.0x）；runnables 全域 6 任务逐类核查 + 不做项论证（[round-18](round-18.md)） | ✅ 完成（服务器回归通过，0 插件异常） |
| 19 | 热循环 Location 分配消除（粒子路径）：ChillWind 球面 1.18x，其余噪声级——**域内实测收敛判定**（无观察者 spawnParticle 廉价 + JIT 逃逸分析消除短命克隆，同类扫荡边际收益可忽略）；位置序列等价性 true（[round-19](round-19.md)） | ✅ 完成（服务器回归通过，0 插件异常） |
| 20 | v0.4.0 全量终验（106 变体，历轮全部等价性断言 true）+ 纯空闲 soak + **收敛判定**（[round-20](round-20.md)） | ✅ 完成（**第三轮循环闭合**，版本收口 0.4.0） |

## 收敛判定（round-20）

第三轮循环覆盖：写路径（15）、展示行（16）、召唤物 AI（17）、周期任务（18）、
热循环分配（19，实测到阈值之下）。叠加前两轮循环已闭合的读路径/判定路径/
事件路径/机械路径/启动路径，全部可识别域均已做完；剩余成本由 Bukkit/Paper
API 边界构成（setItemMeta/spawnParticle/getNearbyEntities/实体注册——次数
由玩法与视觉契约固定），或经实测确认低于可测阈值（Location 分配、目标读取）。
**第三轮性能优化循环于 round-20 闭合，版本收口 0.4.0。**

## 收敛判定（round-14）

世界级事件路径（10/13 覆盖全部 17 监听器）、机械热路径（11 + 首轮 4/5）、
启动冷路径（12）、施法/交互/gadgets/统计（首轮 1-9）均已做完；剩余成本由
Bukkit/Paper/Slimefun API 边界构成，或经实测确认已足够便宜（实体 PDC ~15ns、
getByItem ~20ns）。**第二轮性能优化循环于 round-14 闭合，版本收口 0.3.0。**

## 收敛判定（round-9）

全部识别热路径域（常驻定时器/施法全链路/机械判定/交互监听/gadgets/数据加载/统计）
已做完；剩余成本由 Bukkit/Paper API 边界构成（实体扫描/元数据应用为玩法必需或
引擎侧）。性能优化循环于 round-9 闭合。
