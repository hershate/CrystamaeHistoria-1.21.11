# 性能优化第 60 轮：判定轮——遗留集合/数据结构选型/掉落 API 探针（零发现）

日期：2026-08-17
性质：判定轮（第十一轮循环收敛计数 1/2，r59 重置后重新累计）。探针
角度与 r42-59 互异：**遗留集合类型、数据结构选型、掉落物 API 形态**。

## 探针结果（全库扫描 + 逐点复核，零可行动发现）

1. **遗留集合类型**：`LinkedList`/`java.util.Vector`/`Stack`/
   `Hashtable`/`Enumeration` 全库**零命中**（`org.bukkit.util.Vector`
   为物理向量类不属此类）——数据结构选型全程现代形态；
2. **掉落物 API 形态**：`dropItem`/`dropItemNaturally` 共 10+ 处调用
   点全部位于方块破坏掉落、机关关闭清槽、法术产物等事件/生命周期
   级路径——每次调用即生成真实实体（µs 级 API 边界），两种形态的
   差异（随机偏移）为各站点语义所需；
3. **集合构造形态**：52 处 `new HashMap<>` 中，tick 路径类
   （gadgets/mechanisms/runnables/types）的全部命中均为**实例字段**
   （注册期构造一次，r7/r10 备忘录族），无每 tick 集合构造与
   大量填充场景。

## 判定

本轮零发现（三组探针全空）。第十一轮循环收敛计数 **1/2**（r59
重置后）；下轮（r61）以新角度复核，若再零发现则按 r44 准则宣告
第十一轮循环收敛并执行 0.12.0 版本收口。
