# 性能优化第 43 轮：族探针穷尽判定（长尾地板确认，无代码变更）

日期：2026-08-17
性质：判定轮——汇总复查节奏以来的**调用点族探针矩阵**并核验剩余
未探族，确认性能面已到长尾地板。

## 族探针矩阵（r33 修订以来累计）

| 族 | 轮次 | 结果 | 量级 |
|----|------|------|------|
| 周期落盘（save 序列化） | 34 | 实质优化 | ~283,000x |
| 空域实体扫描 | 35 | 边界确认 | 噪声级 |
| 伤害权限门控 | 37 | 环境依赖边界 | 判定 |
| 玩家循环体（判定前取块） | 38 | 卫生（JIT 已消） | 噪声级 |
| SF 身份解析（getByItem） | 39/40 | 实质优化 | 177-178x |
| tick getItemMeta | 41 先行清扫 | 全事件级 | 无 tick 调用点 |
| getBlockData 双读 | 42 | 边界确认 | 持平（~30ns） |

## 本轮未探族核验

- **反射族**（`Class.forName`/`newInstance`/`getDeclaredMethod`）：
  全库零命中——不存在；
- **菜单槽读取**（液化池 `getItemInSlot`）：r11 已随判定链优化，
  无新增调用点；
- **热路径日志**：机械路径仅失败分支记录且限流（r7 沉淀），
  ChroniclerPanelCache 仅 1 处 getLogger 引用（非热路径）。

## 地板判定

代际递减曲线（283,000x → 178x → 持平）+ 三类剩余归属
（API 边界 / 事件级边际 / 环境依赖）+ 本轮未探族零命中——
**插件侧性能面已到可测地板**。复查节奏的边际产出趋零：后续轮次
预期为纯判定轮（除非外部触发条件出现：Paper/Slimefun 演进、
玩法变更、集成环境实测画像）。

## 建议

维持 0.9.0；循环可降频或停止（触发条件已沉淀于 note/README.md
与 perf/README.md）。若继续逐分钟触发，本轮起将以判定轮为主，
不再为产出而制造变更。
