# 性能优化第 20 轮：v0.4.0 全量终验 + 纯空闲 soak + 收敛判定（第三轮循环闭合）

日期：2026-08-16
基准数据：[benchmark/results/round-20-final.tsv](../../../benchmark/results/round-20-final.tsv)（v0.4.0 全量，106 变体）
红线核查：安全性 ✅ 稳定性 ✅（终验与 soak 均 0 插件异常、0 tick 落后）兼容性 ✅（无数据格式变更）

## 本轮内容（收敛轮，无新优化点）

1. **版本收口**：pom 0.3.0 → 0.4.0（plugin.yml 经 filtering 同步），
   测试服旧 jar 移除后部署 v0.4.0。
2. **全量终验**：v0.4.0 实机跑分（Paper 1.21.11 b132 + Slimefun 5.0.0），
   106 变体（历轮全部基准 + 第 15-19 轮新增），COMPLETE 正常、0 插件异常、
   优雅关停（EXIT=0）；**历轮全部等价性运行时断言（round-13/15/16/17/18/19
   共 12 组）在终验会话中重跑并全部通过**。
3. **纯空闲 soak**：移除基准插件后空载运行 ~10.7 分钟（18:52:01 → 19:02:45）
   ——**0 次 "Can't keep up"、0 次看门狗转储、0 非原版 ERROR、0 插件 WARN**
   （日志中的 yggdrasil/PKIX 为原版服务器访问 Mojang 服务的离线机器噪音，
   与插件无关），优雅关停。
4. 会话健康度：终验 WATCHDOG 4 次与 soak 会话 4 次均为已知基准现象
   （round-8 YAML 双解析、round-15/18 计时批次连续占主线程）；
   脚本计数器偶把看门狗转储中的插件堆栈帧计为 CH_ERROR——人工逐条归因
   确认真实插件异常为 0（帧时间戳与转储一致）。

## 第三轮循环总览（round-15 → 20）

| 轮次 | 域 | 代表性成果 |
|------|----|-----------|
| 15 | 写路径单次元数据往返 | 故事提交 2.47x（8-10 克隆 → 1+1，原子性更优）；提取 1.30x；统计 1.17x；法杖 lore 1.08x |
| 16 | 展示行构建缓存 | displayName 49.5x / loreLines 40.6x / 4 条组装段 9.9x |
| 17 | 召唤物 AI 每 tick | 冲撞车块扫描 13.5x；主人查询 5.4x；GoalType 常量 3.1x |
| 18 | 周期任务路径 | TunnelBore 块扫描 271.4x（每 tick 3.24ms → 12µs） |
| 19 | 热循环分配 | 球面 1.18x，余噪声级——分配域实测收敛 |
| 20 | 收敛收口 | 全量终验 + 纯空闲 soak + 版本 0.4.0 |

方法沉淀：单次元数据往返提交、不可变展示行记忆化、构造互异去重消除、
在线判定单查询、单次克隆可复用坐标（0.4.0 发布说明模式清单 11-15）。

## 收敛判定

第三轮循环覆盖并做完了五个可识别域：写路径（15）、展示行（16）、召唤物
AI（17）、周期任务（18）、热循环分配（19——实测低于可测阈值并给出判定）。
叠加前两轮循环已闭合的读路径/判定路径/事件路径/机械路径/启动路径，
全部可识别优化域均已做完。剩余成本由 Bukkit/Paper API 边界构成
（setItemMeta 应用、spawnParticle、getNearbyEntities、实体生成/注册——
调用次数由玩法与视觉契约固定），或经实测确认低于可测阈值（Location 分配、
实体目标读取）。**第三轮性能优化循环闭合，版本收口 0.4.0。**

上游遗留（不属性能范畴，移交审计轮）："有故事的"名称前缀叠加
（round-15 发现，旧/新行为一致）。

## 变更文件

- pom.xml（0.4.0）
- benchmark/results/round-20-final.tsv（106 变体）
- [note/release/0.4.0.md](../../release/0.4.0.md)（发布说明）
- 本报告 + note/README.md + perf README 索引
