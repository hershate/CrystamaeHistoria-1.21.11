# 审计第 82 轮：Materials 材料族消费点清单核对（判定轮 2/2——收敛休眠第七次）

日期：2026-08-19
范围：全新探针角度——`Materials.java` 24 个材料字段的**消费点清单**
核对（注册即消费的模式验证 + 死字段检测）。

## 清单结果

| 维度 | 结论 |
|------|------|
| **活跃 getter**（3/24） | `getCrystalMap`(5 处：祭坛 saveMap/降级/驱动的晶簇查表)、`getDummyCrystalMap`(4 处：图鉴展示)、`getPrismaticCrystal`(2 处：镀金器/驱动)——全部为高频消费点且既往轮次验证 ✓ |
| **无外部 getter 引用**（23 项） | **非死代码**：这些材料的消费形态为**注册即消费**——`sfItem.register(plugin)` 使其进入 Slimefun 注册表（图鉴展示、配方系统（作为其他物品的合成材料，如融合锭→稀有融合锭链）、Runes 引用 ARCANE_SIGIL/GILDED_PEARL（r79 核对过））——`@Getter` 为 Lombok 统一生成（24 字段一个注解），未使用的外部 getter 属**生成冗余而非设计缺陷**（上游原样） |
| 注册序 | 全部在 `Materials.setup()`（第一注册段，r34 序审）——被 Runes/Tools/Mechanisms 引用时间序正确 ✓ |

**零发现**：无真正的死字段（全部经注册表消费）、无 null 路径
（字段在 setup() 中无条件赋值）、消费点均有既往验证覆盖。

## 判定：零发现（2/2）——循环收敛休眠（第七次）

r81（命令 Tab 补全面）+ r82（材料消费清单形态）连续互异角度零
发现。**休眠待触发**：Paper/Slimefun 演进、玩法变更、负载画像、
用户指示（含真人复核清单执行）。

## 验证

纯代码判定轮（无服务器启动/无进程占用）；业务端口 25565 未触碰。
