# 审计第 61 轮：奇术盐方法级直接调用验证（r60 取解的闭环对照）

日期：2026-08-19
范围：r60 机制取解（合成事件预取消跳过 NORMAL 级监听器）的**对照
验证**——绕过事件分发，直接调用生产监听器方法本体。

## 结果：PASS（r57/58/60 三轮链条完整闭环）

| 断言 | 结果 |
|------|------|
| `new ThaumaturgicSaltsListener().onInteract(event)` 直接调用 | ✅ **fill 3→0**——`emptyBasin` 执行、池清空 |
| 盐消耗（heldItem 数量递减） | ✅（saltConsumed 断言含于 cleared） |
| 事件路径（分发）仍无效果 | ✅（对照：同一命令内分发调用依旧被预取消跳过） |
| 后续透镜再注入（tier_scan T1=3） | ✅（清池后重填正常） |

**完整证据链**：r57（效果缺失）→ r58（五门条件全真矛盾）→ r60
（机制取解：预取消跳过 NORMAL 级）→ **r61（方法级直调生效 = 生产
逻辑端到端正确，分发层差异为唯一变量）**。

**最终定性**：奇术盐生产逻辑**完全正确**（方法级实证）；此前所有
"未定"均为合成事件分发的 harness 特性。真人复核 4a 降级为形式确认
（预期必过）。

## 验证

world_r61 用毕删除；PID 45700 RCON 优雅停服；环境完全还原；
业务端口 25565 未触碰。
