# 审计第 20 轮：自改回归复审 + 长时挂机观察

日期：2026-08-15
范围：19 轮累计代码变更（112 文件，+2166/-277 行）的 diff 级复审 + 测试服 4 分钟 soak test

## 自改复审结论

1. **异常处理审计**：新增 41 个 catch 块中，36 个为精确类型（IllegalArgumentException/IllegalStateException/NumberFormatException——解析与数据损坏语义明确）；5 个 broad `catch (Exception)` 全部为**断路器**（带 WARNING 日志 + 日志限流 + 停用消费者/终止施法），无静默吞错 ✓。
2. **行为变化核对**：唯一刻意行为变化（尊贵物品配方要求放置者在线）已在发布说明记录 ✓。
3. **失败关闭语义一致性**：全部防御路径均为拒绝操作/跳过效果/保守默认值，无"失败后放行"路径 ✓。

## Soak Test 结果（4 分钟完整生命周期）

| 检查项 | 结果 |
|--------|------|
| ERROR/Exception/Caused by | **0** |
| "Can't keep up"（tick 落后） | **0** |
| 插件启用/物品注册 | 274 物品、995 故事（基线一致） |
| 优雅关停 | EXIT=0，数据保存正常 |

## 最终判定

连续 4 轮（17-20）无中危以上发现；自改复审无回归；长时运行观察健康。**审计收敛确认，1.21.11-2 达到发布质量。**

## 验证

`JAVA_HOME=F:/Java/21 mvn -q package` 构建通过。
