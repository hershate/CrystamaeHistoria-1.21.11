# 审计第 81 轮：命令层 Tab 补全与权限矩阵复查（判定轮 1/2）

日期：2026-08-19
范围：全新探针角度——`HistoriaCommand.onTabComplete` 与 5 个子命令的
补全路径权限矩阵（r39 审的是**执行**对抗面；本轮为**补全面**，
角度互异）。

## 矩阵结果

| 维度 | 结论 |
|------|------|
| 一级补全权限过滤 | `canUse(sender)` 前置于 `startsWith`——**op 专属子命令对非 op 不泄露名称** ✓（与 r39 帮助过滤对称） |
| 子命令参数补全守卫 | `subCommand != null && canUse` 双条件——非 op 对 test-spell/test-wand 参数补全返回空表 ✓ |
| 补全项与执行口径一致 | test-spell/test-wand 补全 `spell.getId()`（eca4568 后与 getById 同口径）；仅**已启用**法术（enabledSpells）✓ |
| 无参/未知前缀 | 空表返回（无 NPE：args.length ≥ 2 时 last index 安全）✓ |
| 64 项截断 | 上限防御在位 ✓ |

**零发现**：补全面与执行面（r39 12 用例）权限口径完全对称，
eca4568 的口径统一在补全侧同样生效。

## 判定

零发现（计数 1/2，与 r80 互异：命令交互面形态）。

## 验证

纯代码判定轮（无服务器启动/无进程占用）；业务端口 25565 未触碰。
