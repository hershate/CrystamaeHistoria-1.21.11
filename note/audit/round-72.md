# 审计第 72 轮：293ef90 修复实机验证（消息哨兵双对照）

日期：2026-08-19
范围：r71 修复（`onPoseClone` 补 `ignoreCancelled = true`）的实机
验证——真实事件分发双对照 + 消息哨兵（"该方法仅能对注入盔甲架使用"）。

## 验证设计

- 部署含修复的 0.18.1 构建（清 `.paper-remapped` 缓存，r62 规程）；
- 驱动 `r72`：主手注入 PoseCloner（SEA_PICKLE）+ 生成普通盔甲架，经
  `callEvent` 分发两个真实 `PlayerInteractAtEntityEvent`：
  1. **未取消**（对照：处理器应执行 → 哨兵消息应到达）；
  2. **预取消**（修复目标：`ignoreCancelled=true` 应使处理器跳过 →
     无新消息）。

## 结果：PASS（sentinel_total=1，精确符合期望）

| 分发 | 处理器执行 | 哨兵消息 | 判定 |
|------|-----------|---------|------|
| 未取消事件 | ✅ 执行（normal_fired） | **#1 到达** | 处理器功能不变 ✓ |
| 预取消事件 | **跳过**（修复生效） | **无第二条** | **293ef90 实证** ✓ |

修复前形态（无 `ignoreCancelled`）将产生第二条消息——单哨兵即精确
分辨。会话零插件异常。

## 验证

world_r72 用毕删除；PID 46852 RCON 优雅停服；测试服还原为发布版
0.18.1 jar；业务端口 25565 未触碰。
